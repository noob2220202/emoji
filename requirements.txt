python-telegram-bot[job-queue]==21.6
Pillow>=10.0
python-dotenv>=1.0
opencv-python-headless>=4.9
imageio-ffmpeg>=0.5
rembg[cpu]>=2.0.59
# 가상환경 없이 시스템 파이썬에 설치할 때, apt로 깔린 시스템 cryptography 패키지가
# cffi를 필요로 하는데 누락되어 있는 경우가 있어 명시적으로 추가해둔다.
cffi>=1.16
# 무료 횟수 자정(KST) 초기화에 zoneinfo를 쓰는데, 일부 최소 설치 환경엔 시스템 시간대
# 데이터베이스가 없을 수 있어 순수 파이썬 백업 데이터를 추가해둔다.
tzdata>=2024.1
